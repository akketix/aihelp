---
// Deprecated. The in-browser wiki editor has been retired. Community edits now
// go through GitHub's native "Edit this page" flow (see CustomEditLink.astro and
// the Starlight editLink config), which opens a standard pull request under the
// contributor's own GitHub identity. This component is no longer imported
// anywhere and the /api/submit-edit endpoint it posted to now returns 410 Gone.
// Kept as a placeholder; safe to delete.
---

{/* This component intentionally renders nothing. */}